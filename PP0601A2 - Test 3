#include <iostream>
using namespace std;

int main() {
	
 int number=0;
    int r=0;
    int counter=0;
    while (cin>>number)
    {
        if(number == 42 && r==1)
            counter++;
        if(number!=42)
        {
            r=1;
            cout<<number<<endl;
        }
        else
        {
            r=0;
            cout<<number<<endl;
        }
        if(counter==3)
            break;
	}
}
