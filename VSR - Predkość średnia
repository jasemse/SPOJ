#include <iostream>
using namespace std;

int main() {
	short int tests, v1, v2, averageSpeed;
	cin >> tests;
  while(tests--)
    {
     cin >> v1 >> v2;
     averageSpeed = (2*v1*v2)/(v1+v2);
     cout << averageSpeed <<endl;
    }
    return 0;
}
